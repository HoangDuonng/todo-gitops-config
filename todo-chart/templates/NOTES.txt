1. Get the application URL by running these commands:
{{- if .Values.ingress.className }}
  kubectl get ingress todo-ingress --namespace {{ .Release.Namespace }}
  echo "Visit the application via the Ingress external IP."
{{- else }}
  echo "Visit http://127.0.0.1:8080 to access the Frontend"
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ .Values.todoFe.name }} 8080:80
{{- end }}
